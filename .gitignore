# AI files
.claude/

# Terraform state and local overrides
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl
terraform.tfvars
*.auto.tfvars
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Secrets
*.pem
*.key
*.p12
.env
.env.*

# OS
.DS_Store
Thumbs.db

# Local-only scripts (internal tooling not meant for the public template,
# e.g. organisation-specific compliance tagging with internal identifiers).
scripts/local/
